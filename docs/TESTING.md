# Pruebas

Las reglas del dominio se ejecutan con:

```bash
gradle testDebugUnitTest
```

La compilación de verificación se ejecuta con:

```bash
gradle assembleDebug lintDebug
```

La detección, TTS, permisos, restricciones de batería y apertura de ChatGPT deben validarse en un teléfono físico.
